pipeline{
    agent { label "instance-2"}
    stages{
        stage('git pull'){
            steps{
                checkout scm
                echo "project pulled from git"
            }
        }
    }
}